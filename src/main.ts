let myName: String;
let age: number;
let isHuman: boolean;
let album: any;

myName = 'Lily'
age = 31
isHuman = true
album = 'Eyes Wide Open'

const sum = (a: number, b: string) => {
    return a + b
}

let postId: number | string;
let isActive: boolean | number;

let re: RegExp = /\w+/g